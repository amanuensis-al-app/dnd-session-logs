# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-standalone
dist-ssr
*.local

.claude/settings.local.json

# Session-diary doc for AI-assisted sessions — carries dev-environment/git-identity
# detail not meant for the public repo (untracked + purged from history 2026-07-25).
# Kept locally on each dev machine; sync it yourself, it doesn't travel with git.
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
